{
  "name": "@songbook/book-model",
  "version": "0.0.0",
  "private": true,
  "description": "Pure-TypeScript domain model, parser and search index for .songbook files. No React Native dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "bench": "vitest bench --run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
